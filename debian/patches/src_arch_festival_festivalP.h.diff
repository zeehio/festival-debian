This patch is part of a series of patches for festival for
Debian GNU/Linux, which primarily fix build errors with GCC 4.3 and newer.

Index: b/src/arch/festival/festivalP.h
===================================================================
--- a/src/arch/festival/festivalP.h
+++ b/src/arch/festival/festivalP.h
@@ -50,7 +50,7 @@
 void festival_wfst_init();
 void festival_fringe_init(void);
 
-extern ostream *cslog; 
+extern std::ostream *cslog; 
 
 LISP l_audio_mode(LISP mode);
 void audsp_play_wave(EST_Wave *w);
