This patch is part of a series of patches for festival for
Debian GNU/Linux, which primarily fix build errors with GCC 4.3 and newer.

Index: b/src/modules/donovan/donovan.cc
===================================================================
--- a/src/modules/donovan/donovan.cc
+++ b/src/modules/donovan/donovan.cc
@@ -49,6 +49,8 @@
 #include "festival.h"
 #include "donovan.h"
 
+using namespace std;
+
 static CONFIG *make_config(void);
 static void delete_config(CONFIG *config);
 static SPN *make_spn(EST_Utterance &u);
