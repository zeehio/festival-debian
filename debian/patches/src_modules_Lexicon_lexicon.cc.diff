This patch is part of a series of patches for festival for
Debian GNU/Linux, which primarily fix build errors with GCC 4.3 and newer.

Index: b/src/modules/Lexicon/lexicon.cc
===================================================================
--- a/src/modules/Lexicon/lexicon.cc
+++ b/src/modules/Lexicon/lexicon.cc
@@ -43,6 +43,10 @@
 #include "lexiconP.h"
 #include "lts.h"
 
+using std::cout;
+using std::cerr;
+using std::endl;
+
 static int bl_match_entry(LISP entry,const EST_String &word);
 static int match_features(LISP req_feats, LISP act_feats);
 
