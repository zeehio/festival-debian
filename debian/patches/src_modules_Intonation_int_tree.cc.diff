This patch is part of a series of patches for festival for
Debian GNU/Linux, which primarily fix build errors with GCC 4.3 and newer.

Index: b/src/modules/Intonation/int_tree.cc
===================================================================
--- a/src/modules/Intonation/int_tree.cc
+++ b/src/modules/Intonation/int_tree.cc
@@ -47,6 +47,9 @@
 #include "festival.h"
 #include "intonation.h"
 
+using std::cerr;
+using std::endl;
+
 enum lr_tpos {tp_start, tp_left, tp_mid, tp_right, tp_end};
 
 static EST_String accent_specified(EST_Item *s);
